import React, { useState } from 'react';

const Searchengine = ({ onSearch }) => {

    return (
        <div>
            <h1>Search Engine</h1>
        </div>
    );
};

export default Searchengine;