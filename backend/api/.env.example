# 🔒 PRZYKŁADOWY PLIK KONFIGURACJI
# Skopiuj do .env i uzupełnij własnymi danymi

# Port aplikacji
PORT=3000

# MongoDB - UZUPEŁNIJ własnymi danymi!
DATABASE=your_mongodb_connection_string_here

# JWT Secret - Wygeneruj silny klucz: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_TO_YOUR_SECURE_JWT_SECRET

# Domena aplikacji
DOMAIN=http://localhost:3000

# Środowisko
NODE_ENV=development

# 🔐 EMAIL CONFIGURATION FOR 2FA
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=twoj-email@gmail.com
SMTP_PASS=twoje-haslo-aplikacji-16-znakow
EMAIL_FROM=Twoja App <twoj-email@gmail.com>

# 🧪 EMAIL TEST MODE (true = log to console, false = send real emails)
EMAIL_TEST_MODE=true

# Przykład konfiguracji dla różnych dostawców:
# Gmail: smtp.gmail.com, port 587, użyj hasła aplikacji
# Outlook: smtp.outlook.com, port 587
# Yahoo: smtp.mail.yahoo.com, port 587
# Własny serwer SMTP: ustaw odpowiednie dane

# WAŻNE INSTRUKCJE:
# 1. Dla Gmail:
#    - Włącz weryfikację dwuetapową w swoim koncie Google
#    - Wygeneruj hasło aplikacji: https://myaccount.google.com/apppasswords
#    - Użyj tego hasła aplikacji zamiast zwykłego hasła
#
# 2. Dla Outlook/Hotmail:
#    - Może wymagać włączenia "mniej bezpiecznych aplikacji"
#
# 3. Testowanie:
#    - Sprawdź czy email węchodzi do folderu spam/junk
#    - Upewnij się że firewall nie blokuje połączenia SMTP